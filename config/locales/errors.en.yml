en:
  activerecord:
    errors:
      models:
        repository:
          attributes:
            name:
              is_already_in_use: is already in use
            html_url:
              not_an_url: must be an url
            clone_url:
              not_an_url: must be an url
              not_a_git_clone_url: must end with '.git'
